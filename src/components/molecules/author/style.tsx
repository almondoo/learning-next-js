import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  align-items: center;
`;

const Name = styled.p`
  margin-left: 10px;
`;

const exportDefault = {
  Wrapper,
  Name,
};

export default exportDefault;
