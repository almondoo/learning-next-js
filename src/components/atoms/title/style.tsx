import styled from 'styled-components';
import variable from '../../common/variable';

const Title = styled.h2`
  font-size: 20px;
  color: ${variable.mainColor.darkAshPurple};
`;

const exportDefault = {
  Title,
};

export default exportDefault;
